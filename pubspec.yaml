name: flutter_projects
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 1. ADDED FOR LOCALIZATION [cite: 19, 31, 32]
  flutter_localizations:
    sdk: flutter
  google_fonts: ^6.3.3
  cupertino_icons: ^1.0.6

  # Firebase (Backend)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12

  # State Management & Helpers
  provider: ^6.1.5+1
  # 2. ENSURED INTL IS PRESENT FOR L10N
  intl: ^0.20.2
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # 3. ADDED TO GENERATE LOCALIZATION CLASSES [cite: 72, 73]
  generate: true